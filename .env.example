# Copia este archivo como .env en local.
# En Netlify: Site configuration > Environment variables > Add key/value pairs

# URL base del backend (sin /swagger)
VITE_API_URL=TU_URL_BACKEND

# Ruta del formulario de reportes en esta app
VITE_REPORTS_URL=TU_RUTA_DEL_FORMULARIO_DE_REPORTES
